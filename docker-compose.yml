version: '3.8'
services:
  api:
    build: .
    restart: unless-stopped
    environment:
      PORT: 3000
      CORS_ORIGIN: http://localhost:3000
      RATE_LIMIT_MAX: 60
    ports:
      - "3000:3000"
    volumes:
      - ./operacoes.db:/app/operacoes.db
